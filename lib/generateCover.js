// import { SVG } from "@svgdotjs/svg.js";

// if (typeof window === 'object') {
//   const svg = SVG()
//   svg.rect(100, 100).fill("black")

//   let blob = new Blob([svg.svg()], { type: "image/svg+xml" });
//   let url = URL.createObjectURL(blob);
//   let headerDecoration = document.querySelector(".header-decoration")
//   headerDecoration.style.backgroundImage = `url(${url})`;
//   headerDecoration.addEventListener("load", () => URL.revokeObjectURL(url), { once: true });
// }
